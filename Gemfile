# Gemfile

source "https://rubygems.org"

gem "rspec"        # For writing and running tests
gem "simplecov", require: false  # For test coverage

group :development, :test do
  gem "pry"  # For debugging (optional but recommended)
end